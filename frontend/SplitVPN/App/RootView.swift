import SwiftUI

/// Корневой экран. Авторизация вырезана (тестовый проект без пользователей) —
/// сразу показываем главный экран.
struct RootView: View {
    var body: some View {
        HomeView()
    }
}
